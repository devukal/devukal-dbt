version: 2


models:
  - name: employee
    description: "DBT Model for employee table"
    columns:
      - name: emp_id
        tests:
          - unique
          - not_null
      - name: emp_salary
        tests:
          - value_check
           

sources:
  - name: employee
    database: DBT_DB
    schema: PUBLIC
    tables: 
      - name: EMPLOYEE_RAW
